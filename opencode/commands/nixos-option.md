---
description: Look up a NixOS option with type, default, and docs.
agent: nix-specialist
---

Lookup the NixOS option `$ARGUMENTS` using MCP-NixOS and provide:
1. Whether the option exists and its channel/version availability.
2. The type of the option.
3. The default value.
4. A short example of how to set it in a config.
If the option doesn’t exist, suggest close alternatives.

